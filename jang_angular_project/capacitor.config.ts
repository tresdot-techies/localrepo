import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jang_angular.app',
  appName: 'jang_angular_project',
  webDir: 'dist/jang_angular_project',
  bundledWebRuntime: false
};

export default config;
